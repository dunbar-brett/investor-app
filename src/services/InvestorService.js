/**
 * Service for investor qualification calls
 */
 //import axios from 'axios'

 export default {
    // calculate investor info
   async calculate (payload) {
      console.log(payload);
      return;
      //return axios.post(`${process.env.REACT_APP_API_URL}/comments`, payload)
   }
 }